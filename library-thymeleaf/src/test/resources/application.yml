spring:
  liquibase:
    change-log: db/changelog/db.changelog-test.yaml
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true